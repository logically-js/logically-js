import { Formula } from './Formula';
import { LineOfProof, Proof } from './Proof';
import { CITED_LINES_COUNT, DEDUCTION_RULES } from './constants';

export { Formula, LineOfProof, Proof, CITED_LINES_COUNT, DEDUCTION_RULES };
